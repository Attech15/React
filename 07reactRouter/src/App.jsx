import { useState } from 'react'

import './App.css'

// we use Link in place of a tag in React and whenever we use a tag refresh the page but conceopt of refresh didnot happened in react.
// if we use a tag in react so what is point to use react because page reload ho jata . but we can use a tag attribute with Link in react .
function App() {
  const [count, setCount] = useState(0)

  return (
    <>
     
    </>
  )
}

export default App
