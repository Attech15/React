import Chai from "./chai";

function App() {
  return (
  //  <h1>Chai aur react with vite | Akash </h1>
  // you can expport only one element if you are trying to render multiple elements so error occurs and you should make function UPPERCASE otherwiese error will occures and if we creates new file in src so extension must be jsx if we use vite + react and if we  creates new file in pure react so js/jsx extension must be.
  // -- this is fragment we can return multiple elements wiht this and also we can do that with div tag but we prefer the fragment to do that
  // hello 
  <> 
  <Chai/> 
  <h1>hello world</h1>
  <p>Welcome to your Vite + React app.</p>
  </>
  )
}

export default App
